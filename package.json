{
  "name": "icocalc",
  "version": "0.1.0",
  "private": true,
  "description": "IcoCalc — Supabase-backed calculator app",
  "scripts": {
    "lint": "echo 'No linter configured yet' && exit 0",
    "test": "echo 'No tests configured yet' && exit 0",
    "build": "echo 'No build step configured yet' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
